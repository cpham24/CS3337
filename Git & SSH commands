Generate public/private rsa key pair:
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

Get agent pid for SSH:
eval $(ssh-agent -s)

Add the rsa key to ssh-agent:
ssh-add ~/.ssh/id_rsa

SSH to github:
ssh -T git@github.com
