Generate public/private rsa key pair:
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

Get agent pid for SSH:
eval $(ssh-agent -s)

Add the rsa key to ssh-agent:
ssh-add ~/.ssh/id_rsa

SSH to github:
ssh -T git@github.com

Add everything to commit:
git add .

Check status of Git repo:
git status

Connect current repo to remote repo:
git remote add origin [ssh@git]

Check status of Git remote repo:
git remote -v

Push Git to remote location:
git push -u origin [branch]
git push -u origin master


